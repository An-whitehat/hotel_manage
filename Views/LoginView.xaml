<UserControl x:Class="QLKS.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="#F3F5F9">
        <Border Width="430" Padding="30" Background="White" CornerRadius="16" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Border.Effect>
                <DropShadowEffect BlurRadius="20" ShadowDepth="2" Opacity="0.18"/>
            </Border.Effect>
            <StackPanel>
                <TextBlock Text=" Đăng nhập hệ thống" FontSize="28" FontWeight="Bold" HorizontalAlignment="Center"/>
				<TextBlock Foreground="#666" Margin="0,8,0,25" HorizontalAlignment="Center"/>
                <TextBlock Text="Username" FontWeight="SemiBold"/>
                <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Height="38" Margin="0,6,0,14" Padding="10"/>
                <TextBlock Text="Password" FontWeight="SemiBold"/>
                <TextBox Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}" Height="38" Margin="0,6,0,20" Padding="10"/>
                <Button Content="Đăng nhập" Command="{Binding LoginCommand}" Height="42" Background="#2F3F92" Foreground="White" FontWeight="Bold"/>
                <WrapPanel Margin="0,18,0,0" HorizontalAlignment="Center"/>

            </StackPanel>
        </Border>
    </Grid>
</UserControl>
