<UserControl x:Class="QLKS.Views.RoomView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:QLKS.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900">
    <Grid Margin="16" Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Banner Header -->
        <Border Background="#2F3F92" CornerRadius="12" Padding="18" Margin="0,0,0,12">
            <StackPanel>
                <TextBlock Text="Quản lý danh sách phòng" Foreground="White" FontSize="24" FontWeight="Bold"/>
                <TextBlock Text="Thêm, sửa, xóa thông tin phòng, cập nhật trạng thái và loại phòng" Foreground="#DDE4FF"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="360"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Form panel -->
            <Border Grid.Column="0" Background="White" Padding="14" CornerRadius="10">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="Thông tin phòng" FontSize="18" FontWeight="Bold" Margin="0,0,0,12"/>

                        <TextBlock Text="Số phòng" Margin="0,4,0,2"/>
                        <TextBox Text="{Binding SoPhong, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8" Height="28" VerticalContentAlignment="Center"/>

                        <TextBlock Text="Loại phòng" Margin="0,4,0,2"/>
                        <ComboBox ItemsSource="{Binding ListLoaiPhongCombo}" SelectedItem="{Binding SelectedLoaiPhong}" DisplayMemberPath="TenLoaiPhong" Margin="0,0,0,8" Height="28" VerticalContentAlignment="Center"/>

                        <TextBlock Text="Trạng thái" Margin="0,4,0,2"/>
                        <ComboBox ItemsSource="{Binding ListTrangThai}" SelectedItem="{Binding TrangThai}" Margin="0,0,0,8" Height="28" VerticalContentAlignment="Center"/>

                        <TextBlock Text="Ghi chú" Margin="0,4,0,2"/>
                        <TextBox Text="{Binding GhiChu, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12" Height="60" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"/>

                        <WrapPanel HorizontalAlignment="Center" Margin="0,8,0,0">
                            <Button Content="Thêm mới" Command="{Binding AddPhongCmd}" Margin="4" Padding="15,6" Background="#2ECC71" Foreground="White" BorderThickness="0" Cursor="Hand"/>
                            <Button Content="Cập nhật" Command="{Binding EditPhongCmd}" Margin="4" Padding="15,6" Background="#3498DB" Foreground="White" BorderThickness="0" Cursor="Hand"/>
                            <Button Content="Xóa phòng" Command="{Binding DeletePhongCmd}" Margin="4" Padding="15,6" Background="#E74C3C" Foreground="White" BorderThickness="0" Cursor="Hand"/>
                        </WrapPanel>
                    </StackPanel>
                </ScrollViewer>
            </Border>

            <!-- DataGrid panel -->
            <Border Grid.Column="2" Background="White" Padding="14" CornerRadius="10">
                <DataGrid ItemsSource="{Binding ListPhong}" SelectedItem="{Binding SelectedPhong}" AutoGenerateColumns="False" IsReadOnly="True" RowHeight="32" SelectionMode="Single">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Mã" Binding="{Binding MaPhong}" Width="80"/>
                        <DataGridTextColumn Header="Số phòng" Binding="{Binding SoPhong}" Width="110"/>
                        <DataGridTextColumn Header="Loại phòng" Binding="{Binding LoaiPhong.TenLoaiPhong}" Width="160"/>
                        <DataGridTextColumn Header="Giá phòng (VNĐ)" Binding="{Binding LoaiPhong.GiaPhong, StringFormat={}{0:#.##0}}" Width="140"/>
                        <DataGridTextColumn Header="Số giường" Binding="{Binding LoaiPhong.SoGiuong}" Width="100"/>
                        <DataGridTextColumn Header="Trạng thái" Binding="{Binding TrangThai}" Width="120"/>
                        <DataGridTextColumn Header="Ghi chú" Binding="{Binding GhiChu}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
