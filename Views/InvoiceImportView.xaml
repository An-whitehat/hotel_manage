<UserControl x:Class="QLKS.Views.InvoiceImportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:QLKS.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="1000">
    <Grid Margin="16" Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <!-- Header -->
        <Border Background="#2F3F92" CornerRadius="12" Padding="18" Margin="0,0,0,12">
            <StackPanel>
                <TextBlock Text="Quản lý hóa đơn nhập" Foreground="White" FontSize="24" FontWeight="Bold"/>
                <TextBlock Text="Tạo và quản lý hóa đơn nhập dịch vụ" Foreground="#DDE4FF"/>
            </StackPanel>
        </Border>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="340"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <!-- LEFT PANEL -->
            <Border Grid.Column="0" Background="White" CornerRadius="10" Padding="14">
                <StackPanel>
                    <TextBlock Text="Thông tin hóa đơn" FontSize="18" FontWeight="Bold" Margin="0,0,0,12"/>
                    <TextBlock Text="Ngày lập"/>
                    <TextBox Text="{Binding CurrentInvoice.NgayLap}" IsReadOnly="True" Margin="0,0,0,8"/>
                    <TextBlock Text="Loại hóa đơn"/>
                    <TextBox Text="{Binding CurrentInvoice.LoaiHoaDon}" IsReadOnly="True" Margin="0,0,0,8"/>
                    <TextBlock Text="Ghi chú"/>
                    <TextBox Text="{Binding CurrentInvoice.GhiChu}" Margin="0,0,0,12" Height="80" TextWrapping="Wrap" AcceptsReturn="True"/>
                    <Border Background="#F5F6FA" CornerRadius="6" Padding="10" Margin="0,8">
                        <StackPanel>
                            <TextBlock Text="Tổng tiền" FontWeight="Bold" RenderTransformOrigin="0.507,-0.678"/>
                            <TextBlock Text="{Binding CurrentInvoice.TongTien, StringFormat=\{0:#\,##0\} VNĐ}" FontSize="18" Foreground="#E67E22" FontWeight="Bold"/>
                        </StackPanel>
                    </Border>
                    <WrapPanel HorizontalAlignment="Center" Margin="0,10">
                        <Button Content="Tạo mới" Command="{Binding CreateNewInvoiceCmd}" Margin="4" Padding="15,6" Background="#3498DB" Foreground="White"/>
                        <Button Content="Lưu hóa đơn" Command="{Binding SaveInvoiceCmd}" Margin="4" Padding="15,6" Background="#2ECC71" Foreground="White"/>
                    </WrapPanel>
                </StackPanel>
            </Border>
            <!-- RIGHT PANEL -->
            <Border Grid.Column="2" Background="White" CornerRadius="10" Padding="14">
                <DockPanel>
                    <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,10">
                        <ComboBox Width="250" ItemsSource="{Binding ListDichVu}" SelectedItem="{Binding SelectedDichVu}" DisplayMemberPath="TenDichVu"/>
                        <TextBox Width="80" Margin="10,0" Text="{Binding QuantityToImport}"/>
                        <Button Content="Thêm dịch vụ" Width="120" Command="{Binding AddItemToInvoiceCmd}" Background="#2ECC71" Foreground="White" Margin="10,0"/>
                        <Button Content="In hóa đơn" Width="100" Command="{Binding PrintInvoiceCmd}"/>
                    </StackPanel>
                    <DataGrid ItemsSource="{Binding CurrentDetails}" AutoGenerateColumns="False" Height="235">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Dịch vụ" Binding="{Binding DichVu.TenDichVu}" Width="60"/>
                            <DataGridTextColumn Header="Đơn giá" Binding="{Binding DonGia,StringFormat={}{0:#.##0}}" Width="120"/>
                            <DataGridTextColumn Header="Số lượng" Binding="{Binding SoLuong}" Width="100"/>
                            <DataGridTextColumn Header="Thành tiền" Binding="{Binding ThanhTien,StringFormat={}{0:#.##0}}" Width="150"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </DockPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
