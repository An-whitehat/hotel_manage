<UserControl x:Class="QLKS.Views.PrintGuestListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:QLKS.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid x:Name="PrintArea" Margin="50" Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,0,0,30">
            <TextBlock Text="HAPPY HOTEL" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center"/>
            <TextBlock Text="DANH SÁCH KHÁCH ĐANG LƯU TRÚ" FontSize="24" FontWeight="Bold" Margin="0,10,0,5" HorizontalAlignment="Center"/>
            <TextBlock Text="{Binding Source={x:Static sys:DateTime.Now}, StringFormat='Ngày in: \{0:dd/MM/yyyy\}'}" FontSize="14" FontStyle="Italic" HorizontalAlignment="Center" xmlns:sys="clr-namespace:System;assembly=mscorlib"/>
        </StackPanel>

        <DataGrid Grid.Row="1" ItemsSource="{Binding ListKhachLuuTru}" AutoGenerateColumns="False" CanUserAddRows="False" 
                  Background="White" BorderBrush="Black" BorderThickness="1" HeadersVisibility="Column" GridLinesVisibility="All">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Họ Tên Khách" Binding="{Binding HoTen}" Width="220" />
                <DataGridTextColumn Header="CCCD" Binding="{Binding CCCD}" Width="150" />
                <DataGridTextColumn Header="Số Điện Thoại" Binding="{Binding SDT}" Width="120"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="200" />
            </DataGrid.Columns>
        </DataGrid>

        <Grid Grid.Row="2" Margin="0,50,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                <TextBlock Text="Người lập bảng" FontWeight="Bold" FontSize="16"/>
                <TextBlock Text="(Ký, ghi rõ họ tên)" FontStyle="Italic" Margin="0,5,0,80"/>
            </StackPanel>
            <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                <TextBlock Text="Giám đốc" FontWeight="Bold" FontSize="16"/>
                <TextBlock Text="(Ký, đóng dấu)" FontStyle="Italic" Margin="0,5,0,80"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
